# Convenience file for users who prefer requirements.txt.
# Source of truth is pyproject.toml ([project].dependencies).
# Keep this file in sync with runtime dependencies only.

# Core
rich>=13.0.0
click>=8.0.0
pydantic>=2.0.0
typer>=0.9.0
python-slugify>=8.0.0
tqdm>=4.65.0

# Video downloading
yt-dlp>=2023.12.30

# Network and media processing
requests>=2.31.0
websockets>=12.0
brotli>=1.1.0
certifi>=2023.11.17
ffprobe
ffmpeg
curl_cffi>=0.5.0
mutagen>=1.47.0
xattr>=0.10.1; sys_platform != 'win32'
pycryptodomex>=3.19.0
secretstorage>=3.3.3; sys_platform != 'win32'

# Misc
# sqlite3 is built into Python
pathlib2>=2.3.7
